// src/constants.js

/**
 * Centralized keys for localStorage.
 * Using constants helps prevent typos and makes it easy to update keys in one place.
 */
export const STORAGE_KEYS = {
  USERNAME: 'userName',
  BUDGETS: 'budgets',
  EXPENSES: 'expenses',
};